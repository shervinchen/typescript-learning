{
  const count: number = 123;
  const name: string = "csd";

  class Person {}
  const csdoker: Person = new Person();

  const teacher: {
    name: string;
    age: number;
  } = {
    name: "csdoker",
    age: 18,
  };

  const numbers: number[] = [1, 2, 3];

  const getTotal: () => number = () => {
    return 123;
  };
}
